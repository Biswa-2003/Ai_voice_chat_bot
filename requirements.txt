# AI / LLM
openai>=1.30

# Async HTTP (Deepgram WebSocket + Sarvam TTS + ElevenLabs TTS)
aiohttp>=3.9

# Language detection
langdetect>=1.0.9

# Configuration
python-dotenv>=1.0
pydantic>=2.0
pyyaml>=6.0

# Web server
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12.0

# TTS — Microsoft Edge (free, Hindi + English, no API key needed)
edge-tts>=7.0

# Testing
pytest>=8.0
pytest-asyncio>=0.23
